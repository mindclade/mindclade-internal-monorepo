apiVersion: components.mindclade.dev/v1alpha1
kind: Component
metadata:
  name: protocols
spec:
  type: protocol-contracts
  owner: "@mindclade/platform-runtime"
  maturity: experimental
  visibility: private
  dataClassification: internal
  description: Versioned service, artifact, job, and inference wire contracts.
  dependencies: []
  release:
    mode: source-contracts
    artifact: mindclade-protocols
    version: v1alpha1
    public: false
    controls: [compatibility-review]
